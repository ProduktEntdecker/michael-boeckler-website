# Security-focused npm configuration
# Prevent running install scripts automatically
ignore-scripts=true

# Use exact versions instead of ranges
save-exact=true

# Audit on every install
audit-level=moderate

# Fail on audit issues
audit-level=moderate

# No package-lock changes without explicit upgrade
package-lock=true

# Prefer offline cache when available
prefer-offline=true